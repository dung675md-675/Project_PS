# File này để trống.
# Sự tồn tại của conftest.py tại thư mục gốc giúp pytest
# đây là "rootdir" của project, nhờ đó test_app.py
# (nằm trong thư mục con) có thể import được app_web.py (ở thư mục gốc).